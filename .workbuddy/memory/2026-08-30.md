# 2026-08-30

继续推进 game-mod-tools monorepo 的 JS→TS 重构工作：

- 补齐 nwr/zs2 runtime/bridge 的 package.json 与 tsconfig，将 bridge 构建纳入根 `npm run build`。
- 新增 `scripts/build-bridges.mjs`，规避 npm `--prefix` 连续调用时的段错误。
- 修复 nwr/zs2 tools 的 TypeScript 类型错误（minimist 参数、lz-string 导入、联合类型访问等）。
- 修复 dq2 GUI `bridge.ts` 同时被动态/静态导入的警告。
- 在 NW.js GUI 的 Vite 配置中将 `fs/path/child_process` 标记为 external，消除浏览器兼容性警告。
- 为 save-editor 与 web 工具拆分 jsoneditor 独立 chunk，消除 chunk-size 警告。
- 修正 `packages/ui` Tailwind content 配置，避免匹配各子项目的 node_modules。
- 移除已跟踪的 `dist/` 和 `*.tsbuildinfo`，新增 `.gitignore`（含 vite timestamp 临时文件）。
- 更新 README 改造进度，全部标记完成。
- 完整运行 `npm run build` 通过且无警告，提交变更。

---

本轮继续处理 Kimi Code CLI 因额度/503 暂停后遗留的 nwr-modkit GUI 测试迁移：

- 将 `nwr-modkit/app/gui/tests/*.mjs` 共 19 个测试脚本迁移为 `.ts`，新增 `tests/tsconfig.json`。
- 在 `nwr-modkit/app/gui/package.json` 加入 `tsx` 依赖，用于直接运行 TypeScript 测试。
- 修复多个测试对 React/Vite 改造后源码结构的适配问题：
  - `runtime-state-contract.ts`：将 ESM `sandbox.exports` 映射回命名空间对象。
  - `catalog-contract.ts`：为 transpiled CommonJS 提供 `require` 解析。
  - `build-topology.ts`：改为验证 Vite/React 构建产物，移除旧的 `module:none`/`outFile:app.js` 假设。
  - `guardrails-contract.ts`：为 `App.tsx` 中所有受 A1 护栏覆盖的按钮补充 `id`（如 `goldSetBtn`、`actorAddBtn`、`switchSetBtn` 等）。
- 重新生成 `.omo/evidence/runtime-gate-feature-audit.json`。
- 19 个测试脚本全部通过；`npm run build` 通过。
- 批量更新根目录及 dq2/nwr/zs2 各 modkit 的 README/AGENTS/docs/skills 文档中残留的 `.mjs` 引用为 `.ts`，并将 `node .\tools\...` 示例改为 `npx tsx .\tools\...`。
- 提交 `3ad5ab5`。

---

本轮对项目进行 GitHub 开源友好化整理：

- 统一目录命名风格：
  - `zs2_modkit/` → `zs2-modkit/`
  - `nwr_modkit/` → `nwr-modkit/`
  - `dq2_modkit/` → `dq2-modkit/`
  - `The-Stupendous-World-SaveEdit/` → `tsw-save-edit/`
  - `The-Stupendous-World-SaveEditPro/` → `tsw-save-edit-pro/`
  - `zs2_modkit_template/`、`dq2_modkit_template/` → `zs2-modkit-template/`、`dq2-modkit-template/`
- 根 `package.json` 同步更新 workspaces、scripts 与项目名 `rpgmv-modkit`。
- 共享 UI 库名由 `@game-mod-tools/ui` 改为 `@rpgmv-modkit/ui`，并同步更新所有子项目的依赖与 import。
- 批量更新 README/AGENTS/docs/skills/bridge/scripts 中的旧目录名与包名引用。
- 重写根 `README.md`，增加徽章、功能矩阵、项目结构、历史与免责声明，更适合 GitHub 展示。

---

本轮清理仓库中残留的 `.js` 文件，推进源码全面 TypeScript 化：

- 删除 `tsc`/`vite` 构建产物中的 `.js` 文件（`runtime/bridge/page-bridge.js`、`content.js`、skills 模板中的 bridge 产物等）。
- 更新根 `.gitignore`，忽略 `runtime/bridge/*.js`、`skills/assets/*/runtime/bridge/*.js`、`skills/assets/*/app/gui/app.js` 等生成产物。
- 删除重复的 `vite.config.js`（已存在对应的 `vite.config.ts`）。
- 将手写配置文件从 `.js` 迁移到 `.ts`：
  - `packages/ui/tailwind.config.js` → `tailwind.config.ts`
  - `packages/ui/postcss.config.js` → `postcss.config.ts`
  - `dq2-modkit/app/gui/tailwind.config.js` / `postcss.config.js` → `.ts`
  - `zs2-modkit/app/gui/tailwind.config.js` / `postcss.config.js` → `.ts`
- 修正 `packages/ui` Tailwind content 中残留的 `tsw-save-editPro` 为 `tsw-save-edit-pro`。
- `npm run build` 全量构建通过，验证 TypeScript 配置文件被正确加载。
